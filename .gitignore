# OS files
.DS_Store

# IDE / local tooling (not project content)
.vscode/
.claude/

# Lean build artifacts (Mathlib oleans, ~7G)
.lake/
lake-packages/

# Local-only material (cloned for study, never part of lib history)
references/
web/
